# include <stdio.h>

int main(){

    /* Variables;
     * Variables = A reusable container for storing data values.
     * A variable behaves like the value it holds.
     */

	int age = 25;

	printf("You are %d years old\n", age);

	return 0;
     
}

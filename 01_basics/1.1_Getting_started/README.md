# 01 Basics

**Objective:** Understand the structure of a C program, the compilation process (GCC), and basic output.

##File Manifest
| File | Description |
|------|-------------|
| `hello.c` | Standard Hello World program. |
| `f2c.c` | Fahrenheit to Celsius (Variable basics). |
| `the for statement, symbolic constants, char input and output etc.` | Exploring `<limits.h>` constants. |

## 🧠 Key Concepts
* **Compilation:** Preprocessing -> Compiling -> Assembly -> Linking.
* **Main Function:** `int main(void)` is the entry point.
* **Return Values:** `return 0` implies success; non-zero implies error.
* **Comments:** `//` for single line, `/* ... */` for blocks.

## 📝 Notes
* **Command:** `gcc -o prog_name source.c`

# ⚙️ Concept: [Function Name] (e.g., linked_list_insert)

## 1. Goal and Abstract Data Type (ADT)
* **Function Goal:** What operation does this perform on the data? (e.g., inserts a node at the head of a Linked List).
* **ADT:** What abstract data type is being implemented? (e.g., Stack, Queue, Hash Table).

## 2. Function Signature & Purpose
```c
// int linked_list_insert(Node **head, int data);
/* Returns 0 on success, -1 on failure. */
